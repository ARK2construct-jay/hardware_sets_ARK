# Production Environment Variables
# Copy these to your hosting platform (Render, Heroku, etc.)

NODE_ENV=production
PORT=5000

# MongoDB Atlas connection string
# Get this from MongoDB Atlas dashboard
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/hardware_selection?retryWrites=true&w=majority

# JWT Secret - use a strong, unique secret for production
JWT_SECRET=hardwareSelectionSecretKey2024ProjectFinal

# Frontend URL - update with your Netlify URL
FRONTEND_URL=https://hardware-selection-system.netlify.app